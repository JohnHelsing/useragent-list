import { expect }  from 'chai'
import useragentList from '../src/index'


describe('useragent-list basics', function () {
  it('should exist', function () {
    expect(useragentList).to.be.object
  })
})
